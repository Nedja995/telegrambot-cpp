#pragma once
#include <string>
#include <vector>
namespace npml {
	using namespace std;

	class DetectProfanity {
	public:
		bool hasProfanity(string text);
	//protected:
		// vector<_parseResponse(string responseJSON);
	};
}